apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
spec:
  type : LoadBalancer
  selector:
    project: roboshop
    env: Prod
    component: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80